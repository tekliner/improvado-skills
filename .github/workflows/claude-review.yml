name: Review PR
on:
  pull_request:

jobs:
  review:
    uses: tekliner/reusable-workflows/.github/workflows/claude-code-review.yaml@master
    with:
      pr-number: ${{ github.event.pull_request.number }}
    secrets: inherit
